<div class="row">
	<div class="input-field col s6 offset-s3">
		<ul class="error">
		  <% if @current_user && @current_user.errors.any? %>
		    <% @current_user.errors.full_messages.each do |message| %>
		      <li> <%= message %> </li>
		      <% end %>
		  <% end %>
		</ul>
	</div>
</div>

<div class="row">
	<div class="col s6 offset-s3">
		<% if current_user %>
			<h1>Edit Here</h1>
		<% else %>
			<h1 class="center">Start Shipping Soon</h1>
		<% end %>
		<h1>Customer Account</h1>
	</div>
</div>

<form action="/signup_customer" method="POST">
	<%= erb :'/signup_form' %>

	<div class="row">
		    <div class="col s6 offset-s3 center">
		      <% if current_user %>
		        <button class="btn waves-effect waves-light" type="submit" name="action">Save</button>
		      <% else %>
		        <button class="btn waves-effect waves-light" type="submit" name="action">Submit</button>
		      <% end %>
		    </div>
	</div>
</form>

<div class="row">
		<div class="col s6 offset-s3 center">
			<% if current_user %>
				<form action="/profile" method="POST">
				  <input type="hidden" name="_method" value="delete">
				  <input type="submit" class="btn btn-default btn-xs" value="DELETE ACCOUNT">
				</form>
			<% end %>
		</div>
</div>

<div class="row">
	<div class="col s6 offset-s3 center">
		<span>Are you a driver?<a href="/signup_driver"> Become a driver</a></span>
	</div>
</div>
